name: ng-start-simple
description: Novo projeto angular básico
types:
  - app-template
inputs:
  - label: Project name
    type: text
    name: project_name
    default: project-name

hooks: 
  - type: run
    trigger: before-render
    commands: 
     'npx @angular/cli ng create app'